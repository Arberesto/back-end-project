server:
  port: 8080
spring:
  datasource:
    tasks:
      url: jdbc:postgresql://localhost/taskmanagertasks
      jdbc-url: jdbc:postgresql://localhost/taskmanagertasks
      username: someuser
      password: somepassword
      driver-class-name: org.postgresql.Driver