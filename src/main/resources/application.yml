server:
  port: 8080
spring:
  datasource:
    tasks:
      url: jdbc:postgresql://localhost/eisetasks
      jdbc-url: jdbc:postgresql://localhost/eisetasks
      username: someuser
      password: somepassword
      driver-class-name: org.postgresql.Driver